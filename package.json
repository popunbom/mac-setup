{
  "name": "mac-setup",
  "version": "1.0.0",
  "private": true,
  "description": "macOS Startup Guide (2019, Mojave)",
  "scripts": {
    "init": "gitbook init",
    "start": "gitbook serve",
    "build": "gitbook build",
    "deploy": "gh-pages -d _book -m 'Auto publish book'"
  },
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "gitbook-cli": "^2.3.0"
  }
}
